<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Notes</title>
    <link rel="stylesheet" href="/style2.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Edit Notes</h1>
        <form action="/save" method="post">
            <div class="form-group">
                <h1><%=book.book_name  %></h1>
            </div>
            <input type="hidden" name="bookid" value="<%=book.id%>">
            <div class="form-group">
                <label for="lastReadDate">Last Read Date:</label>
                <input type="date" id="lastReadDate" name="lastReadDate" value="<%=book.last_read  %>" required>
            </div>
            <div class="form-group">
                <label for="rating">Rating (out of 10):</label>
                <input type="number" value="<%=book.rating  %>" id="rating" name="rating" min="1" max="10" required>
            </div>
            <div class="form-group">
                <label for="notes">Notes:</label>
                <textarea id="notes" name="notes" rows="4" required><%=book.notes  %></textarea>
            </div>
            <div class="form-group">
                <button type="submit">Submit</button>
            </div>
        </form>
        <form action="/delete" method="post">
            <button type="submit">Delete</button>
            <input type="hidden" name="bookid" value="<%=book.id%>">
        </form>
    </div>
    <script type="text/javascript">
        textarea = document.querySelector("#notes");
        textarea.addEventListener('input', autoResize, false);

        function autoResize() {
            this.style.height = 'auto';
            this.style.height = this.scrollHeight + 'px';
        }
    </script>
</body>
</html>
